services:
  web:
    build:
      context: ..
      dockerfile: Dockerfile
    image: email-marketing-web:local
    container_name: email_marketing_web
    restart: unless-stopped
    env_file:
      - ../.env.docker
    ports:
      - "3001:3001"